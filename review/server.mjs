import http from 'http';import fs from 'fs';import path from 'path';const root=path.resolve('.');const server=http.createServer((q,r)=>{const f=q.url==='/review-video'?'output/okupy-demo-review.mp4':q.url==='/final-video'?'output/okupy-demo-final.mp4':'review/index.html';const p=path.join(root,f);const st=fs.statSync(p);r.setHeader('Content-Type',f.endsWith('.mp4')?'video/mp4':'text/html; charset=utf-8');r.setHeader('Content-Length',st.size);fs.createReadStream(p).pipe(r)});server.listen(4173,'0.0.0.0',()=>console.log('http://localhost:4173'));
